name: rpmtranslator
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_lints: ^1.0.0
  http:
  url_launcher:
  path:
  io:
  auto_size_text: 3.0.0-nullsafety.0
  split_view:
  oauth2:
  translator: ^0.1.7
  html: ^0.15.0
  file_selector_platform_interface: ^2.0.0
  file_selector_linux:
  file_selector_macos:
  file_selector_windows:
  file_selector:
  highlight_text: ^1.1.0
  universal_io: ^2.0.4
  universal_html: ^2.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
    
flutter:
  uses-material-design: true
  fonts:
  - family: font
    fonts:
       - asset: fonts/NotoSansCJKtc-Regular.otf #Source: https://www.google.com/get/noto/